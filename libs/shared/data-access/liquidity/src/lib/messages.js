/*
 * PoolsContainer Messages
 *
 * This contains all the text for the PoolsContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PoolsContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PoolsContainer container!',
  },
});
